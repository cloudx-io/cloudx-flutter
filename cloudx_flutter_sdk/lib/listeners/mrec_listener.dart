/// MREC ad listener
library;

import 'banner_listener.dart';

/// Listener for MREC (Medium Rectangle) ad events
/// MRECs use banner infrastructure, so they extend [BannerListener]
class MRECListener extends BannerListener {}

